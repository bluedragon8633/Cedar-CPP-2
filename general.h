#pragma once
#include<iostream>
#include<fstream>
#include "SFML/Graphics.hpp"

class General {
public:
	int findInt(std::string name);
	//string getLineFromFile(string fileName, int line);
};

